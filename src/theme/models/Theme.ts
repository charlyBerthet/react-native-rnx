export default interface Theme {
  bgColor: string;
  txtColor: string;
  primaryColor: string;
  secondaryColor: string;
  thirdColor: string;
  txtColorOnPrimaryColor: string;
  borderColor: string;
}
